Звіт з виконання практичного завдання з ООП
Студент: Бистрова Максима Група: КН-31 Варіант: 1 (Агент прогнозу погоди) Посилання на GitHub: [Вставте посилання на ваш репозиторій]

1. Загальна структура проекту
Проект ініціалізовано та налаштовано згідно з вимогами:

Використано менеджер пакетів Poetry (ініціалізовано у віртуальному середовищі).
Встановлені необхідні залежності: google-adk, python-dotenv.
Наявний файл .gitignore (середовище .venv та .env файл ігноруються).
Наявний файл README.md з описом проекту.
Запуск проекту здійснюється командою adk run ..
2. Реалізація ООП парадигм
У файлі agent.py реалізовано систему обліку успішності з демонстрацією всіх 4-х парадигм ООП:

Абстракція: Створено абстрактний клас Person з абстрактним методом get_role(), який визначає загальний інтерфейс для всіх учасників освітнього процесу.
Наслідування: Створено класи Student та Teacher, які успадковують базовий функціонал від класу Person.
Інкапсуляція: У класі Student оцінки зберігаються у приватному списку __grades. Доступ до списку та його модифікація здійснюються виключно через публічні методи класу (add_grade, average, min_grade, max_grade).
Поліморфізм: Метод get_role() перевизначено в класах Student та Teacher так, щоб він повертав різні значення ("Студент" або "Викладач") залежно від об'єкта, який його викликає.
3. Налаштування AI-Агента
Інструмент (Tool): Створено звичайну Python-функцію calculate_grade(name, scores), яка створює об'єкт класу Student, заповнює його оцінками та викликає інкапсульовані методи для розрахунку середнього балу, мін/макс оцінки та визначення літерного грейду (A, B, C, F).
Агент: Налаштовано root_agent за допомогою фреймворку Google ADK. Агенту передано інструкції працювати як освітній асистент, українською мовою, та надано доступ до інструменту calculate_grade.
4. Демонстрація роботи агента (Логи діалогів)
Демонстрація роботи агента
Запитання 1

Користувач: Яка погода у Львові?

Агент:

Місто: Львів
Температура: 18.4 °C
Вологість: 65%
Стан погоди: Хмарно
Порада: Підійде демісезонний одяг.
Запитання 2

Користувач: Яка погода у Києві?

Агент:

Місто: Київ
Температура: 28.1 °C
Вологість: 45%
Стан погоди: Сонячно
Порада: Легкий одяг буде комфортним.
Запитання 3

Користувач: Яка погода в Одесі?

Агент:

Місто: Одеса
Температура: 16.7 °C
Вологість: 88%
Стан погоди: Дощова погода
Порада: Рекомендується взяти парасолю.
Теоретичні питання
1. Що таке абстрактний клас?

Абстрактний клас — це клас, який не можна створити напряму. Він використовується як шаблон для інших класів та може містити абстрактні методи, які обов'язково потрібно реалізувати в дочірніх класах.

У Python для цього використовується модуль abc.

2. Що таке наслідування?

Наслідування дозволяє створювати нові класи на основі вже існуючих.

У цьому завданні:

class TemperatureSensor(Sensor):

та

class HumiditySensor(Sensor):

успадковують властивості та методи класу Sensor.

3. Що таке інкапсуляція?

Інкапсуляція — це приховування внутрішніх даних об'єкта від прямого доступу.

Приклад:

self.__sensors = []

Подвійне підкреслення робить атрибут приватним.

4. Що таке поліморфізм?

Поліморфізм дозволяє викликати однаковий метод у різних об'єктів, отримуючи різну поведінку.

У прикладі:

for sensor in self.__sensors:
    value = sensor.read()

метод read() працює по-різному для різних типів датчиків.

5. Для чого використовується модуль random?

Модуль random використовується для генерації випадкових значень.

Приклади:

random.uniform(-10, 35)

— випадкова температура.

random.randint(20, 100)

— випадкова вологість.
